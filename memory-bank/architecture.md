# 架构说明

## 当前阶段

项目已完成第 13 步：连接电脑自动回合。

当前工程是一个浏览器端本地运行的 Vite + TypeScript + 原生 HTML/CSS 应用。项目已拆分出游戏状态模块、基础牌数据模型、完整牌池生成与洗牌模块、玩家状态模块、初始发牌模块、摸牌模块、打牌模块、回合切换模块、电脑选牌策略模块、电脑自动回合流程模块、界面渲染模块和事件绑定模块。当前已能初始化一局新对局，并支持核心状态层面的摸牌、打牌、合法打牌后的玩家/电脑回合交替、确定性的电脑出牌选择，以及电脑回合中的自动摸牌、打牌并切回玩家；但仍不包含计分、胡牌判断、副露流程或正式界面交互逻辑。

## 根目录文件

### `package.json`

项目 npm 配置文件，声明当前工程为私有前端项目，并配置基础脚本：

- `dev`：启动 Vite 开发服务器。
- `build`：先执行 TypeScript 类型检查，再执行 Vite 生产构建。
- `test`：运行 Vitest 单元测试。

当前开发依赖包括：

- `vite`：前端开发服务器与构建工具。
- `typescript`：类型检查与 TypeScript 支持。
- `vitest`：单元测试框架。
- `jsdom`：为当前 UI 渲染测试提供 DOM 环境。

### `package-lock.json`

npm 依赖锁文件，用于固定依赖解析结果，保证后续开发者安装到一致的依赖版本。

### `index.html`

浏览器入口 HTML 文件。

职责：

- 提供页面基础结构。
- 挂载根节点 `#app`。
- 通过 `<script type="module" src="/src/main.ts">` 加载 TypeScript 入口。

### `tsconfig.json`

TypeScript 配置文件。

职责：

- 启用严格类型检查。
- 使用浏览器 DOM 类型。
- 使用适合 Vite 的 ES 模块与 Bundler 模块解析配置。
- 当前不输出编译产物，构建产物由 Vite 负责生成。

### `vite.config.ts`

Vite 配置文件。

职责：

- 提供 Vite 默认构建与开发服务器配置入口。
- 配置 Vitest 的基础测试环境。
- 当前不引入 React、Vue 或其他前端框架插件。

## `src/` 目录

### `src/main.ts`

当前应用入口文件。

职责：

- 引入全局样式。
- 调用 `createInitialGameState()` 创建初始化状态。
- 查找 `index.html` 中的 `#app` 根节点。
- 调用 `renderApp()` 完成页面渲染。
- 调用 `bindAppEvents()` 建立事件绑定入口。

当前限制：

- 不包含任何麻将规则逻辑。
- 不包含牌山、玩家、计分、AI、胡牌判断或回合状态。
- 后续仍应保持入口职责轻量，只负责初始化和连接模块。

### `src/styles.css`

当前全局样式文件。

职责：

- 设置页面基础字体、背景和排版。
- 为初始化占位页面提供居中展示样式。

当前限制：

- 只服务最小占位页面。
- 尚未定义正式游戏界面布局。

### `src/main.test.ts`

入口与基础渲染测试文件。

职责：

- 验证入口导出的 `initialGameState` 是一份已初始化的新对局状态。
- 验证初始化状态包含玩家回合、玩家与电脑初始手牌以及剩余牌山。
- 验证 `renderApp()` 可以根据当前对局状态渲染标题、说明文案和状态标记。

当前限制：

- 只覆盖入口连接和基础渲染。
- 不覆盖摸牌、打牌、胡牌、计分或正式界面交互。

## `src/game/` 目录

游戏核心逻辑目录。

职责：

- 承载牌定义、牌山、玩家状态、对局状态、规则判断、计分和电脑行动等核心逻辑。
- 核心逻辑不得依赖 DOM 或界面渲染实现。

当前限制：

- 目前包含完整对局状态初始化、基础牌数据模型、完整牌池生成模块、洗牌能力、玩家状态模块、初始发牌模块、摸牌动作、打牌动作、回合切换流程、基础电脑选牌策略和电脑自动回合流程。
- 尚未实现胡牌、计分或副露流程。

### `src/game/game-state.ts`

完整对局状态聚合模块。

职责：

- 定义 `GameStatus`，用于表示对局阶段：`not-started`、`player-turn`、`computer-turn`、`ended`。
- 定义 `Actor`，用于统一表示玩家或电脑这两个行动主体。
- 定义 `WinMethod`，为后续荣和、自摸结算记录胡牌方式。
- 定义 `GameEndResult`，为后续和牌结束与流局结束提供统一结果结构。
- 定义 `DiscardRecord`，用于记录最后打出的实体牌和打牌者。
- 定义 `GameState`，聚合页面标题、说明文案、双方玩家状态、剩余牌山、当前行动方、最后打牌记录、结束结果、海底标记和河底标记。
- 导出 `createNewGameState(randomSource?)`，用于创建一局新对局。
- 导出 `createInitialGameState()`，当前作为入口兼容函数，返回一局新对局状态。

当前新对局初始化流程：

- 使用 `createTilePool()` 创建 48 张完整牌池。
- 使用 `shuffleTilePool()` 洗牌，并允许注入 `RandomSource` 以便测试稳定复现。
- 使用 `dealInitialHands()` 为玩家和电脑各发 13 张初始手牌。
- 初始剩余牌山为 22 张。
- 初始 `status` 为 `player-turn`。
- 初始 `currentActor` 为 `player`。
- 初始 `lastDiscard` 与 `endResult` 为 `null`。
- 初始 `isHaitei` 与 `isHoutei` 为 `false`。

当前限制：

- 只负责完整对局状态结构和新对局初始化。
- 不直接实现摸牌、打牌或回合切换；这些动作已拆分到独立流程模块。
- 尚未实现胡牌判断、计分或副露流程。
- 后续若动作逻辑增多，应优先拆成独立流程模块，避免 `game-state.ts` 变成单体巨文件。
- 对局状态模块不依赖 DOM，也不负责 UI 展示。

### `src/game/tile.ts`

基础牌数据模型模块。

职责：

- 定义本项目允许使用的牌类型：索子与三元牌。
- 定义 `SouzuRank`，限制索子只能是一索至九索。
- 定义 `DragonValue`，限制字牌只能是白、发、中。
- 定义 `SouzuTile`、`DragonTile` 和联合类型 `Tile`。
- 导出 `allowedTiles`，作为当前项目唯一允许牌种列表。
- 导出 `tileIds`，提供稳定的牌 ID 列表。
- 导出 `compareTiles()`，用于手牌、测试和后续规则中的确定性排序。
- 导出 `isSameTile()`，用于按稳定 ID 判断两张牌是否为同一种牌。
- 导出 `getTileById()`，用于根据牌 ID 获取对应牌定义。

当前牌 ID 约定：

- 索子使用 `souzu-1` 至 `souzu-9`。
- 三元牌使用 `dragon-white`、`dragon-green`、`dragon-red`。

当前显示顺序：

- 一索至九索。
- 白、发、中。

当前限制：

- 不包含万子、筒子、东南西北风牌或赤宝牌。
- 只定义牌种，不生成四张实体牌，也不负责洗牌。
- 牌池生成、发牌、摸牌和玩家手牌都应复用这里的牌定义与排序规则。

### `src/game/tile.test.ts`

基础牌数据模型测试文件。

职责：

- 验证允许牌种数量正好为 12。
- 验证允许牌种包含一索至九索、白、发、中。
- 验证不会包含万子、筒子、风牌或赤宝牌。
- 验证同一种牌的比较结果稳定。
- 验证排序后索子按一至九排列，字牌按白、发、中排列。

当前限制：

- 只覆盖牌种定义和排序规则。
- 尚未覆盖发牌、摸牌、玩家状态或回合流程。

### `src/game/deck.ts`

完整牌池生成与洗牌模块。

职责：

- 基于 `src/game/tile.ts` 导出的 `allowedTiles` 生成完整牌池。
- 定义 `TileCopy`，用于表示某一种牌的实体副本。
- 定义 `RandomSource`，用于抽象洗牌时使用的随机数来源。
- 导出 `createTilePool()`，生成未洗牌的完整牌池。
- 导出 `shuffleTilePool()`，对传入牌池执行非破坏性洗牌并返回新的数组实例。
- 导出 `countTileTypes()`，统计牌池中每种牌出现次数。

当前牌池规则：

- 牌池只包含一索至九索、白、发、中。
- 每种允许牌生成 4 张。
- 完整牌池总数为 48 张。
- 每张实体副本通过 `copyIndex` 标记为第 1 至第 4 张。

当前洗牌规则：

- `shuffleTilePool()` 使用 Fisher-Yates 洗牌算法。
- 默认随机来源为 `Math.random`。
- 调用方可以注入自定义 `RandomSource`，用于测试或未来需要确定性牌山的场景。
- 洗牌不会增加、删除或替换任何牌。
- 洗牌不会修改传入的原始牌池数组。

当前限制：

- 不负责发牌、摸牌或牌山状态管理。
- 不负责玩家状态、回合状态、胡牌判断或计分。
- 后续发牌流程应复用 `createTilePool()` 与 `shuffleTilePool()`，不要在发牌模块中重复实现牌池生成或洗牌算法。

### `src/game/deck.test.ts`

完整牌池生成与洗牌测试文件。

职责：

- 验证 `createTilePool()` 生成 48 张牌。
- 验证每一种允许牌正好出现 4 次。
- 验证牌池不会包含非法牌。
- 验证每种牌的副本序号稳定为 1 至 4。
- 验证多次生成未洗牌牌池时组成完全一致，并返回不同数组实例。
- 验证 `shuffleTilePool()` 洗牌后牌数仍为 48。
- 验证洗牌后每种允许牌仍正好出现 4 次。
- 验证使用固定随机来源时洗牌结果可重复。
- 验证洗牌不会修改原始牌池数组。

当前限制：

- 覆盖牌池生成和洗牌能力。
- 尚未覆盖牌山摸牌、发牌流程或回合流程。

### `src/game/player.ts`

玩家状态模块。

职责：

- 定义 `initialPlayerPoints`，作为玩家与电脑的统一初始点数，目前固定为 100000 点。
- 定义 `MeldType`，目前包含 `chi`、`pon`、`kan`，用于表示吃、碰、杠的副露类型。
- 定义 `Meld`，用于记录一组副露及其包含的实体牌。
- 定义 `PlayerState`，用于描述玩家和电脑共享的状态结构。
- 导出 `createPlayerState()`，用于创建一份新的玩家状态实例。

当前玩家状态字段：

- `hand`：手牌，类型为 `TileCopy[]`。
- `discardPile`：牌河，类型为 `TileCopy[]`。
- `melds`：副露列表，类型为 `Meld[]`。
- `points`：当前点数，初始值为 `initialPlayerPoints`。

当前设计约束：

- 玩家与电脑使用同一个 `PlayerState` 结构。
- 每次调用 `createPlayerState()` 都会创建新的数组实例，玩家和电脑状态互不影响。
- 玩家状态模块不依赖 DOM，也不负责 UI 展示。
- 玩家状态模块不负责发牌、摸牌、打牌、胡牌或计分，只提供这些流程需要操作的状态容器。

后续衔接：

- 第 7 步发牌流程应向 `hand` 写入初始手牌，并保持玩家和电脑手牌相互独立。
- 后续打牌流程应从 `hand` 移除实体牌并加入 `discardPile`。
- 后续副露流程应向 `melds` 追加 `Meld`，并从 `hand` 移除副露使用的牌。
- 后续计分流程应更新 `points`，并继续复用 `initialPlayerPoints` 作为新对局起点。

### `src/game/player.test.ts`

玩家状态测试文件。

职责：

- 验证新建玩家状态时点数为 100000。
- 验证新建玩家状态时手牌、牌河和副露列表为空。
- 验证可以独立创建玩家和电脑两份实例，且修改其中一份不会影响另一份。

当前限制：

- 只覆盖玩家状态容器初始化和实例隔离。
- 尚未覆盖发牌、摸牌、打牌、副露操作或计分更新。

## `src/ui/` 目录

界面层目录。

职责：

- 后续承载页面渲染、用户输入事件和 DOM 交互连接逻辑。
- 只读取或传递游戏状态，不应直接实现核心麻将规则。

当前限制：

- 当前只包含初始化页面渲染和事件绑定占位。
- 尚未实现正式游戏布局、手牌点击、胡牌按钮或新对局按钮。

### `src/ui/render.ts`

页面渲染模块。

职责：

- 导出 `renderApp(container, state)`。
- 根据 `GameState` 渲染当前初始化页面。
- 将 DOM 生成逻辑从 `src/main.ts` 中拆出。

当前限制：

- 只渲染标题、说明文案和当前状态标记。
- 后续正式界面应继续让渲染层只负责展示，不直接修改规则状态。

### `src/ui/events.ts`

事件绑定模块。

职责：

- 导出 `bindAppEvents(container)`，作为后续用户操作事件的统一入口。
- 当前返回一个空的清理函数，表示暂未绑定实际事件。

当前限制：

- 尚未绑定任何玩家操作。
- 后续实现打牌、胡牌、新对局等交互时，应在此连接 UI 事件与游戏流程模块。

## 后续架构方向

第 7 步应在 `src/game/` 下实现发牌流程。建议新增独立发牌或对局初始化模块，避免把发牌逻辑塞入 `src/main.ts`、`src/ui/` 或 `PlayerState` 本身。

后续新增规则逻辑时，应继续遵守以下约束：

- `src/main.ts` 只负责初始化和连接模块。
- `src/game/` 不依赖 DOM，不直接处理页面事件。
- `src/ui/` 不实现麻将规则，只负责展示状态和转发用户操作。
- 发牌流程应复用 `src/game/deck.ts` 的 `createTilePool()` 与 `shuffleTilePool()`，不要重复实现牌池生成或洗牌算法。
- 玩家状态应复用 `src/game/player.ts` 的 `PlayerState` 与 `createPlayerState()`，不要在对局状态或 UI 层重复定义玩家字段。
- 发牌后的手牌排序应复用 `src/game/tile.ts` 的 `compareTiles()`，保持展示和测试结果稳定。
- 每个基础模块都应配套测试，避免形成单体巨文件。

## 第 7 步新增架构说明

### `src/game/deal.ts`

初始发牌流程模块。

职责：

- 定义 `initialHandTileCount`，当前固定为 13，表示二人对局中每名角色的初始手牌数量。
- 定义 `dealtPlayerCount`，当前固定为 2，表示初始发牌对象为玩家与电脑。
- 定义 `InitialDealResult`，描述一次初始发牌后的玩家状态、电脑状态和剩余牌山。
- 导出 `sortTileCopies(tileCopies)`，对实体牌数组进行稳定排序。
- 导出 `dealInitialHands(tileWall)`，从传入牌山中完成玩家与电脑的初始发牌。

当前发牌规则：

- 从传入牌山的前 13 张生成玩家手牌。
- 从传入牌山的第 14 至第 26 张生成电脑手牌。
- 发牌后剩余牌山为原始牌山第 27 张及之后的实体牌，当前标准牌池下剩余 22 张。
- 玩家与电脑手牌会按 `src/game/tile.ts` 中的 `compareTiles()` 牌序排序，同一种牌再按 `copyIndex` 排序。
- 发牌函数会创建新的玩家与电脑状态，不修改传入的牌山数组。

依赖关系：

- 依赖 `src/game/deck.ts` 的 `TileCopy` 类型来表示实体牌。
- 依赖 `src/game/player.ts` 的 `createPlayerState()` 与 `PlayerState` 来创建双方状态。
- 依赖 `src/game/tile.ts` 的 `compareTiles()` 来保持手牌排序稳定。

当前设计约束：

- `dealInitialHands()` 不负责创建牌池，也不负责洗牌；调用方应先使用 `createTilePool()` 与 `shuffleTilePool()` 准备牌山。
- 发牌模块不依赖 DOM，也不处理界面展示。
- 发牌模块不负责对局阶段、当前行动方、摸牌、打牌、胡牌或计分。
- 后续完整对局初始化应复用此模块，避免在 `game-state.ts` 或 UI 层重复切牌和排序逻辑。

### `src/game/deal.test.ts`

初始发牌流程测试文件。

职责：

- 验证发牌后玩家手牌为 13 张。
- 验证发牌后电脑手牌为 13 张。
- 验证发牌后标准 48 张牌池剩余牌山为 22 张。
- 验证发牌前后全部实体牌合计仍为 48 张。
- 验证玩家和电脑手牌中不会出现同一实体牌被重复分配。
- 验证发牌后玩家与电脑手牌按固定牌序存储。
- 验证发牌不会修改传入的牌山数组。

当前限制：

- 只覆盖初始发牌流程。
- 尚未覆盖完整对局状态初始化、摸牌、打牌、回合切换、流局、胡牌或计分。

## 第 8 步新增架构说明

### `src/game/game-state.test.ts`

完整对局状态测试文件。

职责：

- 验证新对局初始化后包含玩家与电脑状态。
- 验证玩家与电脑初始手牌均为 13 张，初始点数均为 100000 点。
- 验证新对局初始化后剩余牌山为 22 张。
- 验证新对局初始化后当前行动方为玩家。
- 验证新对局初始化后海底和河底标记都未触发。
- 验证新对局初始化后最后打出的牌为空。
- 验证新对局初始化后对局阶段不是已结束，且结束结果为空。

当前限制：

- 只覆盖完整对局状态初始化。
- 尚未覆盖摸牌、打牌、回合切换、流局、胡牌或计分。

### 第 8 步后的状态流转基础

当前 `GameState` 已经为后续流程预留以下字段：

- `wall`：第 9 步摸牌动作应从这里移除牌。
- `currentActor`：第 9 步和后续回合切换应根据它判断当前行动方。
- `lastDiscard`：第 10 步打牌动作应写入最后打出的牌与打牌者。
- `endResult`：后续胡牌或流局时应写入结束结果。
- `isHaitei`：第 9 步摸到牌山最后一张时应设置为 `true`。
- `isHoutei`：第 10 步在海底摸牌后打出最后一张牌时应设置为 `true`。

后续新增游戏动作时，应保持核心流程在 `src/game/` 内完成，UI 层只负责展示状态和转发操作。


## 第 9 步新增架构说明

### `src/game/draw.ts`

摸牌动作模块。

职责：

- 导出 `drawTile(state)`，根据当前 `GameState` 执行一次摸牌状态转移。
- 根据 `state.currentActor` 判断当前行动方是玩家还是电脑。
- 从 `state.wall` 的第一张取得摸到的实始牌。
- 将摸到的牌追加到当前行动方的 `hand`。
- 将摸到的牌从 `wall` 中移除。
- 当摸到的是牌山最后一张牌时，将 `isHaitei` 标记为 `true`。
- 当牌山为空时，不增加任何手牌，并将对局设置为流局结束。

当前状态转移规则：

- `status === 'ended'` 时调用 `drawTile()` 会直接返回原状态，不继续摸牌。
- `currentActor === null` 时调用 `drawTile()` 会直接返回原状态，不继续摸牌。
- `wall` 非空时，当前行动方手牌增加 1 张，牌山减少 1 张。
- `wall` 为空时，`status` 变为 `ended`，`currentActor` 变为 `null`，`endResult` 写入 `{ type: 'exhaustive-draw' }`。
- 摸牌动作当前不负责胡牌判断、计分、打牌或回合切换。

设计约束：

- 摸牌逻辑放在独立模块中，避免 `src/game/game-state.ts` 形成巨型综合模块。
- 模块不访问 DOM，不处理 UI 事件，也不进行页面渲染。
- 模块只接收并返回 `GameState`，方便后续测试和流程层组合。
- 当前摸到的牌追加到手牌末尾，暂不对摸牌后的手牌重新排序；后续若 UI 需要排序展示，应优先在渲染层或专门的展示转换中处理。

### `src/game/draw.test.ts`

摸牌动作测试文件。

职责：

- 验证玩家摸牌后，玩家手牌增加 1 张，电脑手牌不变，牌山减少 1 张。
- 验证电脑摸牌后，电脑手牌增加 1 张，玩家手牌不变，牌山减少 1 张。
- 验证摸牌使用的是牌山第一张，并且该牌会从牌山中移除。
- 验证摸到牌山最后一张时，`isHaitei` 被标记为 `true`，且不会直接结束对局。
- 验证牌山为空时执行摸牌不会增加任何手牌，并触发流局结束状态。

兼容性注意：

- 当前 `tsconfig.json` 使用 ES2020 标准库，因此测试中不要使用 `Array.prototype.at()` 这类 ES2022 API。
- 如需读取最后一项，应使用 `array[array.length - 1]`。

## 当前第 9 步后的整体架构状态

当前 `src/game/` 已拆分为以下核心模块：

- `tile.ts`：受限牌种定义、牌 ID、展示标签、排序与同牌判断。
- `deck.ts`：完整 48 张牌池生成、实始牌副本定义、洗牌与牌种计数。
- `player.ts`：玩家/电脑共享的手牌、弃牌河、副露、点数状态结构。
- `deal.ts`：初始发牌流程，将洗好的牌山拆分为双方 13 张初始手牌和剩余牌山。
- `game-state.ts`：完整对局状态结构、新对局初始化，以及对局阶段、当前行动方、最后弃牌、结束结果、海底/河底标记等共享状态定义。
- `draw.ts`：基于 `GameState` 的摸牌动作状态转移。

后续新增动作模块时应继续沿用当前拆分方式：

- 打牌动作优先新增为 `src/game/discard.ts`。
- 回合切换优先新增为独立流程或动作模块。
- 电脑策略优先放入 `src/game/ai.ts`。
- UI 层只调用游戏逻辑模块，不直接实现麻将规则或状态转移细节。

## 第 10 步新增架构说明

### `src/game/discard.ts`

打牌动作模块。

职责：

- 导出 `discardTile(state, actor, tileToDiscard)`，根据当前 `GameState` 执行一次打牌状态转移。
- 校验传入的 `actor` 必须等于 `state.currentActor`，非当前行动方不能直接打牌。
- 在 `status === 'ended'` 时直接返回原状态，避免已结束对局继续变化。
- 从当前行动方 `hand` 中移除指定实体牌。
- 将打出的实体牌追加到当前行动方 `discardPile`。
- 写入 `lastDiscard`，记录最后打出的牌与打牌者。
- 当打牌前 `isHaitei` 为 `true` 时，将 `isHoutei` 标记为 `true`。

当前实体牌匹配规则：

- 通过 `tile.id` 与 `copyIndex` 共同判断是否为同一张实体牌。
- 这种判断方式允许后续 UI 或测试传入结构相同但引用不同的实体牌对象。
- 若当前行动方手牌中不存在对应实体牌，`discardTile()` 返回原状态，不修改任何字段。

当前状态转移规则：

- 合法打牌后，当前行动方手牌减少 1 张。
- 合法打牌后，当前行动方牌河增加 1 张。
- 合法打牌后，`lastDiscard` 写入 `{ tile, actor }`。
- 合法打牌后，`isHoutei` 等于打牌前的 `isHaitei`。
- 非当前行动方打牌、打出手牌外实体牌、已结束对局打牌都会返回原状态。
- 打牌动作当前不负责回合切换、胡牌判断、吃碰杠响应、计分或电脑行动。

设计约束：

- 打牌逻辑放在独立模块中，继续保持 `src/game/game-state.ts` 只承担状态结构和新对局初始化职责。
- 模块不访问 DOM，不处理 UI 事件，也不进行页面渲染。
- 模块只接收并返回 `GameState`，方便后续与回合切换、AI、胡牌判断流程组合。
- 第 11 步实现回合切换时，不应把切换逻辑回填到 `discard.ts` 中；更适合新增独立流程模块或动作模块来组合“合法打牌后切换行动方”。

### `src/game/discard.test.ts`

打牌动作测试文件。

职责：

- 验证玩家打出手牌中的牌后，玩家手牌减少 1，玩家牌河增加 1。
- 验证电脑打出手牌中的牌后，电脑手牌减少 1，电脑牌河增加 1。
- 验证最后打出的牌和打牌者记录正确。
- 验证海底摸牌后的弃牌会设置河底标记。
- 验证打出不在手牌中的实体牌会失败，并且返回原状态。
- 验证非当前行动方打牌会失败，并且返回原状态。

## 当前第 10 步后的整体架构状态

当前 `src/game/` 已拆分为以下核心模块：

- `tile.ts`：受限牌种定义、牌 ID、展示标签、排序与同牌判断。
- `deck.ts`：完整 48 张牌池生成、实始牌副本定义、洗牌与牌种计数。
- `player.ts`：玩家/电脑共享的手牌、弃牌河、副露、点数状态结构。
- `deal.ts`：初始发牌流程，将洗好的牌山拆分为双方 13 张初始手牌和剩余牌山。
- `game-state.ts`：完整对局状态结构、新对局初始化，以及对局阶段、当前行动方、最后弃牌、结束结果、海底/河底标记等共享状态定义。
- `draw.ts`：基于 `GameState` 的摸牌动作状态转移。
- `discard.ts`：基于 `GameState` 的打牌动作状态转移。

后续新增动作模块时应继续沿用当前拆分方式：

- 回合切换优先新增为独立流程或动作模块，不要塞入 `game-state.ts`。
- 电脑策略优先放入 `src/game/ai.ts`。
- 胡牌牌形、有役判断、计分应拆成独立规则模块。
- UI 层只调用游戏逻辑模块，不直接实现麻将规则或状态转移细节。


## 第 11 步新增架构说明

### `src/game/turn.ts`

回合切换流程模块。

职责：

- 导出 `discardTileAndSwitchTurn(state, actor, tileToDiscard)`，组合一次打牌动作和合法打牌后的回合切换。
- 导出 `switchTurnAfterDiscard(stateBeforeDiscard, stateAfterDiscard)`，用于在已经完成 `discardTile()` 的场景下单独执行回合切换。
- 合法打牌后，根据 `lastDiscard.actor` 将 `currentActor` 切换为另一方。
- 切换行动方时同步更新 `status`：玩家行动对应 `player-turn`，电脑行动对应 `computer-turn`。
- 非法打牌返回原状态时，不切换行动方。
- 已结束对局不继续切换回合。

当前状态转移规则：

- 玩家合法打牌后，`currentActor` 变为 `computer`，`status` 变为 `computer-turn`。
- 电脑合法打牌后，`currentActor` 变为 `player`，`status` 变为 `player-turn`。
- 如果 `discardTile()` 返回原状态，说明打牌不合法，回合切换模块也返回原状态。
- 如果打牌后状态已经是 `ended`，回合切换模块不会覆盖结束状态。

设计约束：

- 回合切换逻辑不回填到 `discard.ts`，使 `discard.ts` 继续只负责打牌本身。
- 回合切换逻辑不写入 `game-state.ts`，避免对局状态聚合模块膨胀为巨型流程模块。
- 模块不访问 DOM，不处理 UI 事件，也不进行页面渲染。
- 后续电脑自动回合可以复用 `discardTileAndSwitchTurn()` 作为“电脑打牌并交还玩家回合”的基础流程。

### `src/game/turn.test.ts`

回合切换测试文件。

职责：

- 验证玩家合法打牌后当前行动方变为电脑。
- 验证电脑合法打牌后当前行动方变为玩家。
- 验证非法打牌不会切换行动方，并返回原状态。
- 验证已结束状态下不会切换行动方。
- 验证已经完成合法 `discardTile()` 后，也可以通过 `switchTurnAfterDiscard()` 单独切换回合。

## 当前第 11 步后的整体架构状态

当前 `src/game/` 已拆分为以下核心模块：

- `tile.ts`：受限牌种定义、牌 ID、展示标签、排序与同牌判断。
- `deck.ts`：完整 48 张牌池生成、实始牌副本定义、洗牌与牌种计数。
- `player.ts`：玩家/电脑共享的手牌、弃牌河、副露、点数状态结构。
- `deal.ts`：初始发牌流程，将洗好的牌山拆分为双方 13 张初始手牌和剩余牌山。
- `game-state.ts`：完整对局状态结构、新对局初始化，以及对局阶段、当前行动方、最后弃牌、结束结果、海底/河底标记等共享状态定义。
- `draw.ts`：基于 `GameState` 的摸牌动作状态转移。
- `discard.ts`：基于 `GameState` 的打牌动作状态转移。
- `turn.ts`：合法打牌后的玩家/电脑回合切换流程。

后续新增动作模块时应继续沿用当前拆分方式：

- 电脑策略优先放入 `src/game/ai.ts`，且只选择牌，不直接修改状态。
- 电脑自动回合优先放入独立流程模块，组合 `drawTile()`、AI 选牌和 `discardTileAndSwitchTurn()`。
- 胡牌牌形、有役判断、计分应拆成独立规则模块。
- UI 层只调用游戏逻辑模块，不直接实现麻将规则或状态转移细节。

## 第 12 步新增架构说明

### `src/game/ai.ts`

基础电脑出牌策略模块。

职责：

- 导出 `chooseComputerDiscardTile(hand)`，根据传入的电脑手牌选择一张建议打出的实体牌。
- 策略只负责选牌，不直接修改 `GameState`。
- 策略不执行摸牌、打牌、回合切换、胡牌判断或计分。
- 手牌为空时返回 `null`，表示当前没有可打出的牌。

当前选牌规则：

- 先统计手牌中每种牌的数量。
- 数量大于 1 的同种牌不视为孤立牌，避免优先拆对子、刻子或杠子候选。
- 单张字牌视为孤立牌。
- 单张索子若没有相邻索子，则视为孤立牌。
- 存在孤立牌时，选择固定牌序最小的孤立牌。
- 不存在孤立牌时，选择固定牌序最小的手牌作为兜底。
- 同种牌使用 `copyIndex` 排序，保证同一输入下结果稳定。

设计约束：

- `ai.ts` 位于 `src/game/`，不依赖 DOM 或 UI 事件。
- `ai.ts` 不接收完整对局状态，避免电脑策略模块隐式承担状态流转职责。
- 电脑自动回合已在 `computer-turn.ts` 中组合 `drawTile()`、`chooseComputerDiscardTile()` 和 `discardTileAndSwitchTurn()`。
- 后续若升级电脑策略，应继续保持“策略选择”和“状态修改”分离。

### `src/game/ai.test.ts`

基础电脑出牌策略测试文件。

职责：

- 验证固定电脑手牌时策略返回预期牌。
- 验证返回的牌一定存在于电脑手牌中。
- 验证电脑手牌为空时返回 `null`。
- 验证多次输入同一手牌时返回结果一致。
- 验证没有孤立牌时返回固定牌序最小的手牌。

当前限制：

- 只覆盖电脑选牌策略。
- 不覆盖电脑摸牌、打牌、回合切换或胡牌判断；电脑自动回合由 `computer-turn.test.ts` 覆盖。

## 当前第 13 步后的整体架构状态

当前 `src/game/` 已拆分为以下核心模块：

- `tile.ts`：受限牌种定义、牌 ID、展示标签、排序与同牌判断。
- `deck.ts`：完整 48 张牌池生成、实始牌副本定义、洗牌与牌种计数。
- `player.ts`：玩家/电脑共享的手牌、弃牌河、副露、点数状态结构。
- `deal.ts`：初始发牌流程，将洗好的牌山拆分为双方 13 张初始手牌和剩余牌山。
- `game-state.ts`：完整对局状态结构、新对局初始化，以及对局阶段、当前行动方、最后弃牌、结束结果、海底/河底标记等共享状态定义。
- `draw.ts`：基于 `GameState` 的摸牌动作状态转移。
- `discard.ts`：基于 `GameState` 的打牌动作状态转移。
- `turn.ts`：合法打牌后的玩家/电脑回合切换流程。
- `ai.ts`：基础电脑出牌策略，只选择要打出的实体牌，不修改对局状态。
- `computer-turn.ts`：电脑自动回合流程，组合摸牌、AI 选牌、打牌和回合切换。

后续新增动作模块时应继续沿用当前拆分方式：

- 电脑自动回合流程已独立于 `ai.ts`，后续不要把状态流转塞回电脑策略模块。
- 后续接入胡牌时，电脑回合应在摸牌后打牌前增加自摸检查，并在玩家弃牌后增加荣和检查。
- 胡牌牌形、有役判断、计分应拆成独立规则模块。
- UI 层只调用游戏逻辑模块，不直接实现麻将规则或状态转移细节。

## 第 13 步新增架构说明

### `src/game/computer-turn.ts`

电脑自动回合流程模块。

职责：

- 导出 `playComputerTurn(state)`，用于在电脑回合中推进一次完整自动行动。
- 只在 `status === 'computer-turn'` 且 `currentActor === 'computer'` 时执行；否则返回原状态。
- 先调用 `drawTile()` 完成电脑摸牌。
- 如果摸牌后状态已结束，例如空牌山流局，则直接返回结束状态。
- 调用 `chooseComputerDiscardTile()` 根据电脑手牌选择弃牌。
- 调用 `discardTileAndSwitchTurn()` 完成电脑弃牌并切换回玩家回合。

设计约束：

- `computer-turn.ts` 是流程组合层，不直接实现摸牌、AI 选牌、打牌或切换回合的底层细节。
- `computer-turn.ts` 不依赖 DOM 或 UI 事件。
- `computer-turn.ts` 当前不负责胡牌判断、副露或计分；后续应在保持模块职责清晰的前提下接入这些流程。
- 电脑策略仍保留在 `ai.ts`，避免策略模块承担状态修改职责。

当前流程边界：

- 牌山非空时，电脑摸 1 张并打 1 张，手牌数量保持当前流程预期。
- 电脑合法打牌后，`currentActor` 切换为 `player`，`status` 切换为 `player-turn`。
- 牌山为空时，通过 `drawTile()` 进入 `exhaustive-draw`，不会继续选牌或打牌。

### `src/game/computer-turn.test.ts`

电脑自动回合流程测试文件。

职责：

- 验证电脑回合中牌山非空时会先摸牌再打牌。
- 验证电脑回合结束后手牌数量符合当前流程预期。
- 验证电脑牌河增加 1 张，最后弃牌者为电脑。
- 验证电脑回合结束后行动方切回玩家。
- 验证牌山为空时触发流局且不新增弃牌。
- 验证非电脑回合调用电脑自动回合不会修改状态。

后续衔接：

- 第 14 步基础胡牌牌形判断应优先新增规则模块，不应写入 `computer-turn.ts`。
- 第 16 至第 17 步接入荣和和自摸时，可在电脑流程中复用胡牌入口，但仍应保持胡牌判断与电脑回合编排分离。
